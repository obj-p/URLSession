import HTTP
import SwiftUI

struct HTTPView: View {
    var body: some View {
        VStack {
            Text("Hello, world")
        }
        .background(.red)
    }
}

#Preview {
    HTTPView()
}
